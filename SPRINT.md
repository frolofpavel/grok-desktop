# SPRINT — Мультиагентность Grok Desktop (до 100 агентов)

Спринт из 4 фаз. Каждая фаза = инкремент, проверка (type/test/build), отдельный коммит. Не «3.0 одной PR».

Базовая точка (V2): `delegate_task` (1 суб, one-shot, без tools), `delegate_parallel` (≤12, батчи по 4, 5 ролей, abort+60s). Карточки `subagent-run` живые но эфемерные. `chat_sessions` имеет `kind`+`parent_chat_id`. `cost-guard`, `child-kill`, `handoff.ts`, `smart-router` — есть, недокручены.

Ключевой потолок V2: суб-агенты — one-shot текст без инструментов. Фаза 1 снимает этот потолок.

---

## Фаза 1 — Мощность (идеи 4 + 9) ← фундамент
- [x] Суб-агент = agent-loop с инструментами (не one-shot), итерационный лимит (`electron/ai/sub-agent-loop.ts`, MAX_SUB_ITERATIONS=8, per-task timeout 180с).
- [x] Whitelist инструментов по роли (`electron/ai/role-tools.ts`, `getRoleToolset`):
  - researcher / critic / planner → read-only (read_file, search_project, list_directory, find_files, get_project_map, impact_analysis)
  - verifier → read-only + check_diagnostics + run_command (только test/lint/typecheck — `command-policy.isVerifierCommand`)
  - executor → read + apply_patch / write_file (через mode-policy.decide) + run_command (denylist)
- [x] Суб-агентам запрещено делегировать дальше (delegate_* в SUBAGENT_FORBIDDEN_TOOLS — защита от рекурсии, Фаза 4).
- [x] cost-guard сессии прокинут в sub-loop (ctx.subCostGuard) + родительский abort убивает субов и их run_command-процессы (per-task AbortController, child-kill через ctx.tools).
- [x] Идея 9: provider per task в delegate_parallel — grok (API) + grok-cli (Grok Build) смешиваются в одном батче; CLI-суб работает с loop'ом (буферизация уже в grok-cli).

## Фаза 2 — Прозрачность и масштаб (идеи 1 + 7 + 6)
- [x] Персистентные суб-сессии (kind='subagent', parent_chat_id, история, снапшот). Переключение как в SideChat.
- [x] Панель/вкладка Agents: живой граф/список запущенных+завершённых, фильтры (роль/провайдер/статус), «притащить» результат в основной чат.
- [x] Параллелизм: приоритетные очереди, группы, массовая отмена по роли/тегу, cost-cap на весь батч. До 20–50 агентов без смерти провайдеров/UI.

## Фаза 3 — Оркестрация (идеи 2 + 5 + 8)
- [x] TodoGate: главный агент создаёт todo-лист, субы берут/обновляют/закрывают. Новая таблица + tools.
- [x] Smart-оркестратор: авто-декомпозиция плана по ролям + умный выбор провайдера/модели (smart-router + effortLevel) на подзадачу.
- [x] Per-sub memory + handoff: суб сохраняет находки, на завершении авто-handoff ключевых выводов главному.

## Фаза 4 — Вершина (идеи 3 + 10)
- [x] Динамическое дерево делегирования: субы вызывают delegate_* с лимитом глубины и общего количества + визуализация дерева.
- [x] Agent Swarms: рой по одной цели (исследование+критика+реализация+верификация) с голосованием/консенсусом и выбором лучшего.

---

## Журнал
- (старт) baseline V2 закартирован, план утверждён Павлом («делай под ключ весь спринт»).
- Фаза 1 ✅ — суб-агенты получили tool-enabled agent-loop с whitelist по роли. Новые модули: `sub-agent-loop.ts` (облегчённый цикл, переиспользует tool-handlers.lookupHandler — не дублирует выполнение tools), `role-tools.ts` (getRoleToolset). `command-policy.isVerifierCommand` — гейт run_command для verifier. delegate_task/delegate_parallel переведены с one-shot на loop; delegate_task получил параметр `role`. Идея 9: mixed providers per task (grok+grok-cli) в одном батче. cost-guard сессии учитывает токены субов, abort/timeout (180с) их убивает, delegate_* запрещены субам. UI: карточка subagent-run показывает роль + счётчик tool-вызовов. Тесты: `tests/ai/role-tools.test.ts` (31 кейс). type ✅ build ✅ test:fast ✅ (только sqlite ABI-шум).
- Фаза 3 ✅ — оркестрация. **Идея 2 (TodoGate):** новая таблица `session_todos` (миграция 13) — НЕ переиспользую `tasks` (те — плоские persistent проектные задачи id/text/done; TodoGate — эфемерный сессионный лист со status-enum pending/in_progress/done/blocked, goal, assignee_call_id, ord). Новый `storage/session-todos.ts` (createBatch/update/list/findByTitle, sessionId = parentChatId). Tools `todo_create` (batch) / `todo_update` (по id или title) / `todo_list` → handlers + HANDLER_REGISTRY; эфемерное событие `todo-updated` для live-панели. role-tools: planner→todo_create+todo_update+todo_list, executor/researcher/verifier→todo_update+todo_list, critic→todo_list. UI: секция TodoGate в `AgentsPanel.tsx` (живой прогресс ✅/⏳/⛔/☐, поллинг 2с) + CSS `gg-todogate*`. IPC `agents:todos` (handler+preload+api.d.ts+тип SessionTodo). **Идея 5 (orchestrate):** новый tool `orchestrate` (только главный агент, в SUBAGENT_FORBIDDEN_TOOLS) — декомпозиция цели через вызов модели-планировщика (`decomposeGoal`→чистый `parseDecomposition`), создаёт todo-лист, запускает подзадачи через sub-queue (семафор + cost-cap + группа = callId). Умный выбор модели на КАЖДУЮ подзадачу: smart-router `estimateComplexity`→`recommendModel` (простая→grok-4-fast, сложная→grok-4). **Идея 8 (per-sub memory + handoff):** researcher/verifier/executor получили `memory_save` в whitelist; orchestrate + delegate_parallel субам в финале промптят СТРУКТУРИРОВАННЫЙ итог (СДЕЛАЛ/НАШЁЛ/РЕКОМЕНДУЮ) → главный получает сжатые выводы, а не простыни. Тесты: `tests/ai/orchestrate.test.ts` (11 — parseDecomposition + smart-router per-subtask), `tests/storage/session-todos.test.ts` (6), +Phase 3 кейсы в `role-tools.test.ts` (orchestrate forbidden, todo/memory доступ). type ✅ build ✅ test:fast ✅ (377/377 — в этом прогоне safe-rebuild выровнял sqlite ABI, sqlite-тесты тоже зелёные).
- Фаза 4 ✅ — вершина. **Идея 3 (дерево делегирования):** новый чистый модуль `electron/ai/delegation-limits.ts` — константы `MAX_DELEGATION_DEPTH=2` (главный=0, его суб=1, под-суб=2; делегировать может только depth<2) и `MAX_TOTAL_AGENTS_PER_SESSION=100` (потолок числа агентов за прогон ai:send) + `checkDelegationAllowed(depth,total,count)` (чистый гейт обоих лимитов) + класс `SessionAgentCounter` (атомарный резерв квоты на ВСЁ дерево). delegate_* убраны из `SUBAGENT_FORBIDDEN_TOOLS` (там остались только orchestrate/swarm — оркестрация верхнего уровня) и стали условно-разрешёнными: `getRoleToolset(role, {depth})` добавляет delegate_task/delegate_parallel ролям executor/researcher/planner ТОЛЬКО при depth<MAX; critic/verifier — листовые. Глубина прокидывается через ToolContext: главный loop ставит `delegationDepth:0`, каждый delegate/orchestrate/swarm handler передаёт субу `delegationDepth+1` + `parentCallId` (для дерева) + общий `agentCounter`. Гейт стоит ДВАЖДЫ (defence-in-depth): в whitelist (getRoleToolset) + в начале каждого handler'а через `agentCounter.tryReserve(depth, count)` (батч резервируется целиком → вложенный fan-out не обходит потолок). Cost-cap батча и общий семафор sub-queue уже покрывают всё дерево (вложенные субы идут через тот же subAgentQueue и тот же ctx.subCostGuard). Персист: миграция 14 (`sub_depth` + `sub_parent_call_id`). **Визуализация:** `buildAgentTree` в AgentsPanel.tsx раскладывает плоский список в иерархию по callId↔parentCallId (защита от циклов — visited-set), рендер с отступами по level + ветка ↳ + бейдж глубины `d{n}`; при активном фильтре — плоско. **Идея 10 (Agent Swarms):** новый tool `swarm` (только главный агент, в SUBAGENT_FORBIDDEN_TOOLS) — чистый `buildSwarmRoster(size)` собирает рой по ОДНОЙ цели (researcher-разведчик + N executor-вариантов с РАЗНЫМИ углами-стратегиями + critic), все атакуют цель независимо целиком; затем агент-АРБИТР (read-only critic) собирает варианты и СИНТЕЗИРУЕТ консенсус → возвращает главному. Фоллбэк: арбитр упал → отдаём все варианты главному. Рой уважает очередь/семафор/cost-cap/лимиты глубины+количества; в UI помечается бейджем 🐝 (sub_group=callId роя, task начинается с `[swarm]`). Событие subagent-run получило поле `swarm`. Тесты: `tests/ai/delegation-limits.test.ts` (12 — depth-гейт пускает на <MAX и режет на MAX, счётчик total режет на потолке, цикл не уходит в бесконечность), `tests/ai/swarm.test.ts` (10 — ростер по size, разные углы, арбитр read-only, depth-гейт delegate_*), обновлён `role-tools.test.ts` (контракт Фазы 4: delegate_* под гейтом глубины, orchestrate/swarm всегда запрещены субам). type ✅ build ✅ test:fast — non-sqlite зелёные (67/67 в выборке ai-тестов), sqlite-шум по NODE_MODULE_VERSION (dev запущен → safe-rebuild пропущен, .node залочен) — известная регрессия-не-регрессия.
- **🏁 СПРИНТ ЗАВЕРШЁН** — все 4 фазы (8 идей мультиагентности) сделаны: мощность (tool-enabled субы + whitelist), прозрачность+масштаб (персист-сессии + панель + очередь), оркестрация (TodoGate + smart-orchestrator + handoff), вершина (дерево делегирования с лимитами + рой с консенсусом).
- Фаза 2 ✅ — прозрачность + масштаб. **Идея 1 (персистентные суб-сессии):** миграция 12 — `kind='subagent'` + колонки `sub_role/sub_status/sub_task/sub_group/sub_tool_count/sub_cost_cents/sub_call_id/sub_started_at/sub_ended_at`. Новый `storage/sub-sessions.ts` (create/update/listByProject/get); turns суба пишутся в `chats` (session_id = суб-сессия) → переживают перезагрузку + cascade delete по parent_chat_id. delegate_task/delegate_parallel создают суб-сессию и сохраняют промпт+ответ. **Идея 7 (панель Agents):** `src/components/AgentsPanel.tsx` + вкладка «Агенты» (Sidebar/App). Живой список (поллинг 2с): роль/провайдер/модель/статус/длительность/🔧tool-count/стоимость, фильтры роль/провайдер/статус, клик → просмотр истории суб-сессии (read-only), «↪ Притащить в чат» через CustomEvent `gg-inject-prompt`. **Идея 6 (очередь/отмена/cost-cap):** `electron/ai/sub-queue.ts` — глобальный семафор `GLOBAL_SUB_CONCURRENCY=6` + реестр для массовой отмены по group/role; delegate_parallel MAX_PARALLEL 12→50 (держатся в очереди, стримят по 6); group-тег + cost-cap батча (`cost_cap_usd`, дефолт $3 = DEFAULT_BATCH_COST_CAP_CENTS) → при превышении отмена остатка группы. Новый IPC `agents:list/history/cancel/queue-stats` (handler `ipc/agents.ts` + preload + api.d.ts). Тесты: `tests/ai/sub-queue.test.ts` (5, green), `tests/storage/sub-sessions.test.ts` (5, падают по ABI как и прочие sqlite). type ✅ build ✅ test:fast — non-sqlite green, sqlite-шум (7 файлов, +5 моих по ABI).
